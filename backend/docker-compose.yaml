version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: school-db
    restart: always
    environment:
      POSTGRES_DB: school
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"